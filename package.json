{
  "name": "deskis-calc",
  "version": "1.0.0",
  "devDependencies": {
    "typescript": "^3.8.3"
  },
  "scripts": {
    "build": "tsc -p ./",
    "copy" : "cp src/css/*.css dest/css",
    "start": "npm run build -- -w && npm run copy"
  }
}
